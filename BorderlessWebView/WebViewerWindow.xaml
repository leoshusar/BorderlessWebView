<Window x:Class="BorderlessWebView.WebViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BorderlessWebView"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Title="BorderlessWebView" Height="720" Width="1280"
        Loaded="Window_Loaded">

    <Window.InputBindings>
        <KeyBinding Modifiers="Ctrl" Key="E" Command="{x:Static local:WebViewerWindow.ShowExtensionsCommand}" />
        <KeyBinding Modifiers="Ctrl" Key="T" Command="{x:Static local:WebViewerWindow.ToggleAlwaysOnTopCommand}" />
        <KeyBinding Modifiers="Ctrl" Key="W" Command="{x:Static ApplicationCommands.Close}" />
    </Window.InputBindings>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox
            x:Name="extensionList"
            ItemsSource="{Binding Extensions}"
            DisplayMemberPath="Name"
            Width="240"
            Visibility="Collapsed"
            SelectionChanged="ExtensionList_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsEnabled" Value="{Binding OptionsPath, TargetNullValue=False}"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        
        <wv2:WebView2
            Name="webView"
            Grid.Column="1"
            d:Source="about:blank" />
    </Grid>
</Window>
